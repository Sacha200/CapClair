# Dépendances / build
node_modules/
dist/
coverage/

# Client Prisma généré (artefact de build — Prisma 7 le génère dans le dépôt)
src/generated/

# Secrets et config locale
.env
.env.*
!.env.example

# Fichiers importés par les utilisateurs (jamais versionnés).
# Ancré à la racine de back/ : ne masque PAS src/server/storage/ (code source).
/uploads/
/storage/
